node_modules/
dist/
__pycache__/
*.log
.DS_Store

# 项目级 Agent 配置与数据（含 API Key）；由 migrate 生成，勿提交
.infiniti-agent/
.env.local
# setup:liveui 复制到仓库根时与示例重复，保留本地即可
/model_dict.json

# ASR / TTS 模型文件（体积过大，不纳入 Git）
models/

# MOSS-TTS-Nano 源码克隆（仅本地 serve / pip install -e 用）
third_party/MOSS-TTS-Nano/

# VoxCPM 独立 venv（setup-voxcpm-venv.sh）
third_party/voxcpm-venv/

# Live2D 模型资源（体积较大，本地从 Open-LLM-VTuber 同步，默认不提交）
live2d-models/**
!live2d-models/README.txt

# 兼容文件由用户在工作目录自行维护，不纳入本仓库
AGENT.md
AGENTS.md
CLAUDE.md
.claude/

# 仓库根下的本地工具 / 实验用 Python venv（勿提交）；不要影响 src/tools 等发行代码
/tools/
