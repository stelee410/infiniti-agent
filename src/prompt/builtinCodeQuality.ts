/**
 * 与「写代码」任务相关的通用质量约定；随产品迭代可调整，不占用 SOUL.md。
 * 项目专属栈、命令、目录仍以 INFINITI.md 为准。
 */
export const BUILTIN_CODE_QUALITY_SECTION = `## 内置：编写高质量代码

- **先读再改**：用文件工具看清现有模块、类型与调用关系后再动手；新代码与仓库已有**命名、分层、错误处理方式、导入风格**保持一致。
- **改动聚焦**：只改完成任务所需的代码，避免顺带大重构、删注释或改无关格式；用户明确要求清理时再扩大范围。
- **正确性与可维护性**：优先清晰的数据流与边界条件；避免魔法数与深层嵌套；公共 API 行为变化时同步更新调用方与类型。
- **类型与校验**：在 TypeScript/带类型项目中保持类型收紧，少用 \`any\`；对用户输入、外部数据做合理校验或收窄类型。
- **错误与资源**：错误信息应对调试有用；异步与 I/O 注意超时、取消与资源释放（句柄、监听器），避免吞掉异常。
- **安全**：不把密钥写入仓库或日志；拼接命令/SQL/路径时考虑注入与转义；谨慎处理不可信 URL 与下载内容。
- **可验证**：改动涉及行为时，在说明中点出如何验证（运行何命令、看何现象）；若项目有测试/静态检查，优先在合理范围内跑通或说明未跑原因。
- **INFINITI 优先**：与**本仓库技术栈、测试命令、禁止事项**冲突时，以 **INFINITI.md** 与用户明确指令为准。`
